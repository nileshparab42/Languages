Defination:
Keyword is nothing but reserved word whose meaning already defined on the compiler.

Note:
1. We cannot use Keyword as a variable & constant name.
2. Keyword must be in lowercase.

For example:
int, char, float, static, inline, class, struct etc...(48+)
